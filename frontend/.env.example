# Internal trace:
# - Wrong before: the frontend referenced Vite env vars without shipping a colocated example.
# - Fixed now: the active API base URL is documented next to the Vite app.

VITE_API_URL=http://localhost:8000
